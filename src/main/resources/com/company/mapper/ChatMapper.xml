<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.ChatMapper">

	<resultMap type="com.company.domain.ChatRoom" id="chatRoomResultset">
		<id property="roomId" column="ROOM_ID" />
		<result property="userNickname" column="USER_NICKNAME" />
		<result property="userId" column="USER_USERID" />
		<result property="userPic" column="USER_PIC" />
		<result property="masterNickname" column="MASTER_NICKNAME" />
		<result property="masterId" column="MASTER_ID" />
		<result property="masterPic" column="MASTER_PIC" />
	</resultMap>

	<resultMap type="com.company.domain.ChatMessage" id="chatMessageResultSet">
		<id property="messageId" column="MESSAGE_ID" />
		<result property="roomId" column="ROOM_ID" />
		<result property="message" column="MESSAGE_CONTENT" />
		<result property="userid" column="USER_ID" />
		<result property="nickname" column="USER_NICKNAME" />
		<result property="unReadCount" column="UNREAD_COUNT" />
	</resultMap>

	<!-- 신규 채팅방일 때 채팅 방 생성 -->
	<insert id="createChat" parameterType="com.company.domain.ChatRoom">
		insert into CHATROOM
		values(SEQ_CHATROOM_ID.NEXTVAL, #{userEmail},
		#{userName}, DEFAULT,
		#{masterNicname}, #{masterName}, #{masterPic})
	</insert>

	<select id="selectChatRoom" parameterType="string" resultMap="chatRoomResultSet">
		SELECT * FROM CHATROOM
		WHERE ROOM_ID = #{roomId}
	</select>

	<insert id="insertMessage" parameterType="com.company.domain.ChatMessage">
		<if test="sessionCount == 1">
			INSERT INTO CHATMESSAGE VALUES(#{roomId},
			SEQ_CHATMESSAGE_ID.NEXTVAL,
			#{message}, #{name}, #{email}, DEFAULT)
		</if>
		<if test="sessionCount == 2">
			INSERT INTO CHATMESSAGE VALUES(#{roomId},
			SEQ_CHATMESSAGE_ID.NEXTVAL,
			#{message}, #{name}, #{email}, 0)
		</if>
	</insert>

	<select id="messageList" parameterType="string" resultMap="chatMessageResultSet">
		SELECT * FROM CHATMESSAGE
		WHERE ROOM_ID = #{rooomId}
	</select>

	<select id="searchChatRoom" parameterType="com.company.domain.ChatRoom" resultMap="chatRoomResultSet">
		SELECT * FROM CHATROOM
		WHERE USER_EMAIL = #{userEmail} AND
		MASTER_EMAIL = #{masterEmail}
	</select>

	<select id="chatRoomList" parameterType="string" resultMap="chatRoomResultSet">
		SELECT * FROM CHATROOM
		WHERE USER_EMAIL = #{userEmail} OR
		MASTER_EMAIL = #{userEmail}
	</select>

	<select id="selectUnReadCount" parameterType="com.company.domain.ChatMessage" resultType="_int">
		SELECT COUNT(*) FROM CHATMESSAGE
		WHERE ROOM_ID = #{roomId}
		AND USER_EMAIL != #{email} AND UNREAD_COUNT = 1
	</select>

	<update id="updateCount" parameterType="com.company.domain.ChatMessage">
		UPDATE CHATMESSAGE
		SET
		UNREAD_COUNT = 0
		WHERE ROOM_ID = #{roomId} AND USER_EMAIL != #{email}
		AND UNREAD_COUNT = 1
	</update>

</mapper>